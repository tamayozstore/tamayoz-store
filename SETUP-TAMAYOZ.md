# إعداد النسخة الجديدة من التميز ستور

النسخة دي معمولة بحيث الموقع يشتغل بالمنتجات القديمة حتى قبل توصيل Supabase، وبعد التوصيل المنتجات والإدارة تبقى من قاعدة البيانات.

## 1) مهم قبل أي حاجة

- **مش مطلوب ترفع أي صور منتجات ولا `logo.png` على GitHub.**
- ملف SQL يدخل المنتجات الحالية بأسماء الصور القديمة مؤقتًا فقط عشان لوحة الإدارة تعرف تطابقها.
- بعد توصيل Supabase افتح `admin.html` واستخدم قسم **نقل الصور القديمة إلى Supabase مرة واحدة**: اختار كل الصور من جهازك مرة واحدة، والنظام يضغطها WebP ويرفعها ويربطها بالمنتجات تلقائيًا.
- كل الصور الجديدة بعد كده تترفع من لوحة الإدارة مباشرة إلى Supabase Storage.

## 2) اعمل مشروع Supabase

1. افتح Supabase واعمل New project.
2. اختار اسم للمشروع وكلمة سر Database قوية.
3. استنى لحد ما المشروع يجهز.

## 3) شغّل ملف قاعدة البيانات

1. من Supabase افتح **SQL Editor**.
2. افتح ملف `supabase-setup.sql` اللي في الباكدج دي.
3. انسخ الملف كله والصقه في SQL Editor.
4. اضغط **Run** مرة واحدة.
5. في آخر النتيجة المفروض تشوف `products_count` وقيمته حوالي **35**.

الملف بيعمل:
- جدول المنتجات.
- جدول الطلبات.
- صلاحيات RLS.
- Bucket اسمه `products` للصور.
- إدخال المنتجات اللي موجودة في الملفات الأصلية تلقائيًا.

## 4) اعمل حساب Admin فقط

1. من Supabase افتح **Authentication > Users**.
2. اعمل/ابعت دعوة لإيميلك كـ Admin واضبط كلمة السر.
3. من إعدادات Authentication اقفل **Allow new users to sign up** بعد إنشاء حسابك.
4. خليك سايب Anonymous sign-ins مقفولة.

الفكرة إن سياسات الإدارة تسمح للمستخدم المسجل فقط، ومع قفل التسجيل العام يبقى حسابك هو حساب الإدارة.

## 5) بيانات الربط متضبطة بالفعل

النسخة الجاهزة دي فيها `Project URL` و`Publishable key` اللي بعتهم، ورقم واتساب `01011703785` مضبوط بصيغة دولية داخل `config.js`. **مش محتاج تعدل `config.js` حاليًا.**

المفتاح الموجود Publishable ومخصص للاستخدام من الواجهة العامة مع RLS. **ممنوع تحط Service Role / Secret key في `config.js` أو GitHub.**

## 6) ارفع الملفات على GitHub

ارفع محتويات فولدر `tamayoz-store-ready` إلى **جذر الريبو** واستبدل الملفات القديمة لما GitHub يسألك.

الملفات المهمة:

- `index.html`
- `products.html`
- `admin.html`
- `styles.css`
- `config.js`
- `supabase-client.js`
- `products-fallback.js`
- `store.js`
- `cart.js`
- `admin.js`
- `0-12-months.html`
- `1-2-5-years.html`
- `3-5-years.html`
- `6-11-years.html`

وارفع `supabase-setup.sql` و`SETUP-TAMAYOZ.md` كمرجع لو حابب، مش ضروريين لتشغيل واجهة الموقع.

## 6.5) انقل الصور القديمة مرة واحدة

1. بعد الـDeploy افتح `admin.html` وسجل دخول الأدمن.
2. في قسم **نقل الصور القديمة إلى Supabase مرة واحدة** اضغط اختيار الملفات.
3. اختار **كل صور المنتجات القديمة مرة واحدة** من جهازك. لازم تفضل أسماء الملفات الأصلية كما هي عشان المطابقة التلقائية.
4. اضغط **نقل وربط كل الصور القديمة**.
5. استنى لحد ما تظهر رسالة إن كل الصور اتنقلت بنجاح.
6. بعد كده الموقع يعتمد على روابط Supabase فقط للمنتجات، ومش محتاج صور على GitHub.

## 7) Vercel

لو Vercel مربوط بالـGitHub repo زي وضعك الحالي، أول ما تعمل Commit على `main` هيعمل Deployment تلقائيًا.

بعد ما يخلص:

- افتح الصفحة الرئيسية وتأكد إن المنتجات المميزة ظهرت.
- افتح `products.html?category=0-12` وتأكد إن الـ30 منتج ظهروا.
- افتح `admin.html` وسجل بإيميل الأدمن.

## 8) استخدام لوحة الإدارة

### إضافة منتجات كتير مرة واحدة

1. افتح `admin.html`.
2. اختار الفئة الافتراضية.
3. من **صور المنتجات** اختار 10 أو 20 أو أكتر مرة واحدة.
4. هيظهر صف لكل صورة.
5. اكتب الاسم والسعر، وعدل الفئة لو منتج مختلف.
6. فعل "مميز" لو عايزه يظهر في الرئيسية.
7. اضغط **رفع وحفظ كل المنتجات**.

الصور بتتحول إلى WebP وتتضغط قبل الرفع لتقليل الحجم والتحميل.

### تعديل منتج

من جدول المنتجات اضغط **تعديل**، وغير الاسم أو السعر أو الفئة أو الصورة أو المخزون.

### إخفاء منتج

من التعديل شيل علامة **ظاهر في المتجر** بدل ما تحذفه.

### نفاد المخزون

شيل علامة **متاح في المخزون**؛ المنتج يفضل ظاهر لكن زر الشراء يتحول إلى "نفد من المخزون".

### الطلبات

لما العميل يؤكد الطلب، الموقع يحاول يسجله في Supabase ثم يفتح واتساب بنفس طريقة الموقع القديمة. من لوحة الإدارة هتشوف الطلبات وتغير حالتها.

## 9) لو حصلت مشكلة

### Admin بيقول Supabase لسه مش متوصل
راجع `config.js` وتأكد إنك استبدلت القيم الوهمية ثم عملت Commit.

### تسجيل الدخول بيرفض
راجع Authentication > Users وتأكد إن المستخدم موجود ومؤكد وعنده كلمة سر.

### رفع الصور بيرجع permission error
أعد تشغيل `supabase-setup.sql` وتأكد إن Bucket `products` موجود والسياسات اتعملت.

### المنتجات القديمة ظاهرة لكن Admin مش شايفها
ده معناه غالبًا الموقع شغال بالـfallback؛ راجع `config.js` واتصال Supabase.

## 10) خطوة اختيارية بعدين

بعد ما تتأكد إن النسخة الجديدة مستقرة، ممكن ننقل الصور القديمة نفسها من GitHub إلى Supabase Storage. وقتها تقدر تمسح الصور القديمة من الريبو ويخف حجمه جدًا، لكن **ما تعملش ده قبل تحديث روابط الصور القديمة**.


## تحديث إصلاح Storage والفئة
إذا ظهر `Bucket not found` في رفع منتج أو اللوجو، **لا تعيد تشغيل ملف supabase-setup.sql** على متجر فيه بيانات حالية. شغّل بدلًا منه `RUN-THIS-ONCE-FIX.sql` مرة واحدة؛ هو ينشئ Bucket الصور ويصلح صلاحياته ويطبع فئة «من سنة لسنتين ونص» بدون حذف المنتجات أو الطلبات.

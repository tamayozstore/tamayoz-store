# Tamayoz Store — وضع الحفاظ على الصور والبيانات القديمة

## الفكرة

النسخة دي تفصل بين **القديم** و**الجديد** بدون ما تخسر حاجة:

- المنتجات والصور الموجودة حاليًا في GitHub تفضل كما هي.
- بيانات المنتجات القديمة تتسجل/تكتمل في Supabase بدون إعادة رفع الصور.
- المنتجات الجديدة التي تضيفها من لوحة الإدارة تُحفظ في Supabase، وصورها تُرفع إلى Supabase Storage.
- اللوجو يُعرض من Supabase مع كسر الـcache عند تحديثه.

## لا تمسح الصور القديمة

عند رفع النسخة على الريبو الحالي، استبدل ملفات HTML/JS/CSS فقط. الصور الموجودة في الريبو ليست موجودة داخل ZIP الجديد، وبالتالي ستظل كما هي طالما لم تحذفها بنفسك.

## أول دخول إلى Admin

بعد الـDeploy افتح:

`/admin.html`

بعد تسجيل الدخول، النظام يفحص تلقائيًا المنتجات القديمة الموجودة في `products-fallback.js`. إذا كانت هناك منتجات ناقصة في جدول `products`، يتم إدخال **البيانات فقط** باستخدام اسم ملف الصورة المحلي الموجود على GitHub.

يوجد أيضًا زر داخل قسم الصور واللوجو باسم **فحص واستيراد المنتجات القديمة الناقصة** لو أردت تشغيل الفحص يدويًا.

## المنتجات القديمة بعد المزامنة

بعد المزامنة تصبح المنتجات القديمة موجودة داخل جدول `products` وتظهر في لوحة الإدارة، لذلك تقدر:

- تحدد منتج أو مجموعة منتجات.
- تحذف المحدد دفعة واحدة.
- تغيّر الفئة جماعيًا.
- تغيّر الأسعار جماعيًا.
- تخفي/تظهر المنتجات.
- تغيّر حالة المخزون.
- تجعل مجموعة منتجات مميزة.

حذف منتج قديم من قاعدة البيانات **لن يحذف ملف الصورة القديم من GitHub**. ده مقصود عشان ما نخسرش الملفات الحالية بالغلط.

## المنتجات الجديدة

من قسم **إضافة منتجات بالجملة**:

1. اختر عدة صور مرة واحدة.
2. اكتب الاسم والسعر والفئة.
3. اضغط رفع وحفظ.
4. الصور الجديدة تتحول WebP وتترفع إلى Supabase Storage تلقائيًا.
5. لا تحتاج GitHub للصور الجديدة بعد كده.

## Supabase

بيانات الاتصال موجودة بالفعل في `config.js`. استخدم Publishable Key فقط في الواجهة، ولا تضع Secret/Service Role key في GitHub.

إذا كنت قد شغلت `supabase-setup.sql` من قبل، **لا تحتاج إعادة تشغيله لهذا التحديث**. أول دخول للأدمن سيضيف المنتجات القديمة الناقصة فقط.

لو بتبدأ Supabase من الصفر، شغّل `supabase-setup.sql` مرة واحدة؛ النسخة الحالية منه تحتوي أيضًا على المنتجات الإضافية الموجودة حاليًا في الريبو.

## Vercel

بعد Commit على GitHub، انتظر Deployment الجديد في Vercel ثم اعمل `Ctrl + Shift + R`.

## النتيجة

- القديم: GitHub images + Supabase product data.
- الجديد: Supabase product data + Supabase images.
- مفيش إعادة رفع للصور القديمة.
